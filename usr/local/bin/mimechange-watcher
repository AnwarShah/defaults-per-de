#!/bin/sh

watched_dir="$HOME/.config/"

inotifywait -m -e close_write,moved_to --format %e/%f $watched_dir |
while IFS=/ read -r events file; do
  if [ "$file" == "mimeapps.list" ]; then
    /usr/local/bin/mime-save
  fi
done
